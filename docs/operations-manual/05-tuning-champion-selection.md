# Section 5 — Hyperparameter Tuning & Champion Selection

This section covers the two end-of-cycle ML steps that turn raw backtest output
into production-quality forecasts:

1. **Hyperparameter Tuning** — Bayesian search over per-algorithm and
   per-cluster hyperparameters that feed the next backtest cycle.
2. **Champion Selection** — meta-learner-driven, per-DFU model assignment that
   produces a single "champion" forecast from the candidate model pool.

---

## 5.1 Tuning Pipeline Overview

### Purpose

Tuning runs Bayesian (Optuna) hyperparameter search over each tree-family
algorithm using month-based expanding walk-forward CV. The winning parameter
set is written back into `config/forecast_pipeline_config.yaml` (or its
per-cluster sibling, `config/cluster_tuning_profiles.yaml`) and is consumed by
the **next** backtest cycle.

### Configuration Sources

| File | Role |
|---|---|
| `config/forecast_pipeline_config.yaml` (`tuning:` section) | Global tuning controls (`n_trials`, CV splits, gap, random seed, output dir) |
| `config/forecast_pipeline_config.yaml` (`algorithms.<id>.params`) | Per-algorithm starting params; updated by promotion of best trial |
| `config/tune_strategies.yaml` | Named strategy overrides (LGBM, CatBoost, XGBoost) for `auto-tune` workflow |
| `config/cluster_tuning_profiles.yaml` | Per-cluster overrides; auto-generated by `tune_cluster_hyperparams.py` |
| `config/hyperparameter_tuning.yaml` (search_space_ref) | Optuna search space definitions |

### Key `tuning:` settings (from `forecast_pipeline_config.yaml`)

```yaml
tuning:
  n_trials: 50                # Optuna trial budget
  n_splits: 5                 # walk-forward CV folds
  gap_months: 2               # causality gap between train_end and val_start
  val_months_per_fold: 3
  min_train_months: 13
  early_stopping_rounds: 50
  n_estimators_max: 2000
  random_seed: 42
  output_dir: data/tuning
  search_space_ref: hyperparameter_tuning.yaml
```

### Code Layout

| Module | Role |
|---|---|
| `common/ml/tuning.py` | Shared CV split generator, stabilised WAPE objective, Optuna helpers, JSON load/save |
| `common/ml/tuning_tracker.py` | Tuning-run registry + leaderboard |
| `scripts/tune_hyperparams.py` | Global per-algorithm tuning entrypoint |
| `scripts/tune_cluster_hyperparams.py` | Per-cluster tuning entrypoint (writes `cluster_tuning_profiles.yaml`) |
| `scripts/ml/auto_tune.py` | Strategy-driven auto-tune (consumes `tune_strategies.yaml`) |

---

## 5.2 Run All Tuning — `make tune-all`

Runs Bayesian tuning sequentially for all three core tree algorithms.

| Target | Trials | Notes |
|---|---|---|
| `make tune-lgbm` | 50 (default) | LightGBM global tune |
| `make tune-catboost` | 50 (default) | CatBoost global tune |
| `make tune-xgboost` | 50 (default) | XGBoost global tune |
| `make tune-all` | 50 each | `tune-lgbm` + `tune-catboost` + `tune-xgboost` sequentially |

Each run writes a JSON trial archive to `data/tuning/` and (on promotion)
updates `algorithms.<id>.params` in `config/forecast_pipeline_config.yaml`.
The next `make backtest-all` cycle picks up the new params automatically.

---

## 5.3 Per-Family Tuning — Customer-Enriched Tree Models

Customer-enriched variants (`lgbm_cust`, `catboost_cust`, `xgboost_cust`) use
the 34 pre-computed customer-derived features from
`make customer-features`. They are backtested via `make backtest-cust-enriched-all`.

> **Note (verify in your local Makefile):** A dedicated
> `make tune-cust-enriched-all` target is **not** defined in the current
> Makefile. To tune the customer-enriched family you must invoke the script
> directly with the appropriate algorithm IDs:
>
> ```bash
> uv run python scripts/tune_hyperparams.py --model lgbm_cust
> uv run python scripts/tune_hyperparams.py --model catboost_cust
> uv run python scripts/tune_hyperparams.py --model xgboost_cust
> ```
>
> If a per-family tune target is added, register it under the
> `tune-cust-enriched-all:` rule and add it to the help block.

---

## 5.4 Per-Cluster Tuning

Per-cluster tuning runs an independent Optuna study **per `ml_cluster`** and
writes the resulting overrides into `config/cluster_tuning_profiles.yaml`.
Profiles are matched at backtest time via `resolve_cluster_params()` in
`common/ml/backtest_framework.py`.

### Make targets

| Target | Trials | Description |
|---|---|---|
| `make tune-lgbm-clusters` | 30 | Per-cluster LGBM (each `ml_cluster` tuned independently) |
| `make tune-catboost-clusters` | 30 | Per-cluster CatBoost |
| `make tune-xgboost-clusters` | 30 | Per-cluster XGBoost |
| `make tune-clusters` | 30 each | All three per-cluster tuners sequentially |

### Profile resolution order (`resolve_cluster_params`)

`common/ml/backtest_framework.py:144` → `resolve_cluster_params(cluster_label, stats)`:

1. **Phase 1 — `cluster_name` exact match.** Profile entries with
   `match_criteria.cluster_name: <label>` win first when the label matches the
   `ml_cluster` value exactly.
2. **Phase 2 — Statistical fallback.** Remaining profiles are matched against
   the cluster's statistical fingerprint (`mean_demand`, `cv_demand`,
   `zero_demand_pct`, `intermittency`, `seasonality_strength`).
3. **First match wins** in the order defined by `_PROFILE_PRIORITY` in
   `backtest_framework.py`.

### Profile shape

```yaml
cluster_profiles:
  high_volume_periodic:
    description: Auto-tuned (41220 rows, WAPE=…)
    match_criteria:
      cluster_name: high_volume_periodic     # Phase 1 key
    overrides:
      learning_rate: 0.115
      num_leaves: 57
      max_depth: 12
      n_estimators: 550
      ...
```

### Intermittent-cluster routing reminder

Clusters with `> backtest.intermittent_threshold` (default 0.70) zero-demand
rows are routed to a rolling-mean baseline at backtest time and skip the
tree-model tuning entirely. See `forecast_pipeline_config.yaml` →
`backtest.baseline_intermittent`.

---

## 5.5 Champion Selection — `make champion-all`

Champion selection produces a **per-DFU best-model assignment** from the
candidate forecast pool already loaded into `fact_candidate_forecast`.

### Three-stage pipeline (`make champion-all`)

| Stage | Make Target | Script | Output |
|---|---|---|---|
| 1. Train meta-learner | `champion-train-meta` | `scripts/run_champion_meta_training.py` | Random-forest classifier predicting per-DFU winning model |
| 2. Simulate strategies | `champion-simulate` | `scripts/simulate_champion_strategies.py` | Per-strategy accuracy vs ceiling, written to `data/champion/` |
| 3. Select champion | `champion-select` | `scripts/run_champion_selection.py` | `data/champion/dfu_assignments.csv` and `champion_summary.json` |

`make champion-all = champion-train-meta + champion-simulate + champion-select`.

### Configuration (`forecast_pipeline_config.yaml` → `champion:`)

```yaml
champion:
  strategy: per_cluster
  strategy_params:
    min_prior_months: 5
  fallback_model_id: seasonal_naive
  metric: accuracy_pct
  lag: execution
  min_sku_rows: 3
  min_dfu_rows: 3
  champion_model_id: champion
  promote_gate:
    enabled: true
    min_wape_improvement_pct: 1.0
    min_coverage_frac: 0.8
    bypass_token: null
  meta_learner:
    model_type: random_forest
    n_estimators: 200
    max_depth: 15
    test_months: 3
    performance_window: 6
  models:                     # candidate model pool
    - chronos2
    - nbeats
    - nhits
```

### Strategies

All strategies live in `common/ml/champion_strategies.py` and register
themselves via `@register_strategy("name")`. Every strategy is **strictly
causal** — selection for month `T` uses only data from months
`< T - execution_lag`. This blocks two leakage paths:

1. Future-actuals leakage (standard).
2. Actuals not yet available at forecast issuance time (`fcstdate = T - L`).

---

## 5.6 Champion vs Single Promotion

Both promotion modes go through the same endpoint:

```
POST /backtest-management/{model_id}/promote
```

| Mode | `model_id` | DFU Coverage | Source File |
|---|---|---|---|
| **Single** | A specific algorithm (e.g. `lgbm_cluster`) | All DFUs use the **same** model | Rows copied from `fact_candidate_forecast` WHERE `model_id = <id>` |
| **Champion** | `champion` (per `champion_model_id` config) | Per-DFU best from assignments file | `data/champion/dfu_assignments.csv` joined back to `fact_candidate_forecast` |

The promoted rows land in `fact_production_forecast` and an audit row is
written to `model_promotion_log` with `promotion_type = single | champion`.

### Promote gate

When `champion.promote_gate.enabled = true`, the endpoint blocks promotion
unless **both** of the following hold:

- `min_wape_improvement_pct` over the currently-active promotion is met.
- `min_coverage_frac` of DFUs are represented in the new forecast.

A `bypass_token` may be configured to override the gate for emergency
rollouts.

---

## 5.7 Expert Panel Testing

The Expert Panel evaluates **algorithm-selection quality** end-to-end across a
sampled DFU population, without modifying production tables. It is the
preferred sanity check before promoting a new champion or new candidate model.

### Make targets (verified in `Makefile`)

| Target | Scope | Approx Runtime |
|---|---|---|
| `make expert-panel` | 5000 DFUs × 5 timeframes | ~30 min |
| `make expert-panel-quick` | 1000 DFUs × 3 timeframes | ~8 min |
| `make expert-panel-mini` | 200 DFUs × 2 timeframes | ~2 min |
| `make expert-panel-loc LOC=<location>` | All DFUs at one location | varies |
| `make adv-expert-panel` | 5000 DFUs × 10 TFs, execution-lag accuracy, foundation + DL + statistical upgrades | longer |
| `make adv-expert-panel-quick` | 1000 DFUs × 5 TFs, advanced | medium |
| `make adv-expert-panel-mini` | 200 DFUs × 2 TFs, advanced | short |
| `make adv-expert-panel-loc LOC=<location>` | All DFUs at one location, advanced | varies |

Use the `quick` / `mini` variants for iteration; reserve the full `expert-panel`
and `adv-expert-panel` runs for pre-promotion gates.

---

## 5.8 Stale Tuning Invalidation

When upstream data changes (new sales months, new clusters, schema migration)
existing per-cluster profiles can become stale. The platform exposes an
admin endpoint to clear the stale flag in bulk so the next tuning run will
overwrite the row instead of skipping it.

### Endpoint

```
POST /admin/tuning/invalidate-stale
```

Defined in `api/routers/platform/admin.py`. Guarded by `require_api_key`.

### Behaviour (degrading)

| DB State | Result | Returned `status` |
|---|---|---|
| `cluster_tuning_profile.stale` column exists, rows with `stale = TRUE` present | `UPDATE ... SET stale = FALSE` | `ok` (with `cleared` count) |
| Column exists, no stale rows | No-op | `ok` (cleared = 0) |
| Column does NOT exist | Logged no-op (warning) | `noop` |
| `psycopg.Error` (transient DB issue) | Logged exception, no-op | `noop` |

The endpoint **never raises** on schema absence — it returns `status: "noop"`
so callers (UI buttons, scheduled jobs) can continue uninterrupted.

---

## 5.9 Frontend Integration

The tuning and champion workflows surface in three React tabs:

| Tab | Path | Purpose |
|---|---|---|
| **Lgbm Tuning** | `frontend/src/tabs/lgbm-tuning/` | Trigger LGBM auto-tune runs, view the leaderboard, preview/promote winning params, restore from backup |
| **Model Tuning** | `frontend/src/tabs/model-tuning/` (`ModelTuningTab.tsx`) | Cross-algorithm tuning dashboard (LGBM / CatBoost / XGBoost) — list runs, compare runs, view per-run metrics |
| **Champion Experiments** | `frontend/src/tabs/champion/` | Manage champion experiments (create, run, simulate, view per-strategy accuracy vs ceiling, promote winning experiment) |

### Tuning Chat

A built-in **tuning chat** assistant (`/lgbm-tuning` chat panel + backend
`api/routers/forecasting/tuning_chat.py`) lets operators query tuning history,
ask "why did this trial degrade?", and inspect leaderboard deltas
interactively. This is the primary surface for ad-hoc tuning debugging.

---

## 5.10 Verification & Re-run Cadence

### When to re-tune

| Trigger | Recommended Action |
|---|---|
| New monthly sales data loaded (`make pipeline-refresh`) | Re-run `make tune-clusters` only if cluster fingerprints have shifted; otherwise rely on existing profiles |
| Significant data drift detected (`scripts/ml/detect_drift.py`) | `make tune-all` then `make tune-clusters` |
| New features added to `dim_sku` | `make tune-all` (search space sees new feature distributions) |
| New algorithm added to roster | Tune that algorithm in isolation: `uv run python scripts/tune_hyperparams.py --model <id>` |
| Schema migration touching `cluster_tuning_profile` | `POST /admin/tuning/invalidate-stale`, then re-run `make tune-clusters` |
| Ad-hoc strategy exploration | `make lgbm-auto-tune RUNS=N` (uses `tune_strategies.yaml`) |

### When to re-select champion

| Trigger | Recommended Action |
|---|---|
| Completion of a `make backtest-all` cycle | `make champion-all` (refreshes meta-learner + assignments) |
| New candidate model loaded into `fact_candidate_forecast` | `make champion-all` to re-evaluate; verify with `make expert-panel-quick` before promoting |
| Champion promote gate blocks a promotion | Inspect `model_promotion_log` and the gate thresholds in `forecast_pipeline_config.yaml` → `champion.promote_gate`; consider a `bypass_token` only after manual sign-off |
| Production accuracy regression observed | `make adv-expert-panel-quick` to diagnose, then `make champion-all` |
| Cluster boundaries changed (re-clustering) | `make champion-all` (per-DFU assignments depend on cluster strategy) |

### Verification checklist after re-tune / re-champion

1. `make test-all` — backend + frontend test suites pass.
2. `make audit-routers` — confirms routes/proxies still aligned.
3. Inspect `data/tuning/<model>_<timestamp>.json` for the promoted trial.
4. Inspect `data/champion/champion_summary.json` and `dfu_assignments.csv` for
   coverage and per-strategy ceiling deltas.
5. Spot-check the **Lgbm Tuning** and **Champion Experiments** tabs for the
   new run rows.
6. Promote via `POST /backtest-management/{model_id}/promote` and confirm the
   `model_promotion_log` row.
7. Run `make expert-panel-quick` to validate selection quality before opening
   the gate to a full `make expert-panel`.

---

## Appendix A — Key Files Quick Reference

| Path | Role |
|---|---|
| `common/ml/tuning.py` | CV splits, stabilised WAPE objective, Optuna helpers |
| `common/ml/tuning_tracker.py` | Tuning-run registry + leaderboard |
| `common/ml/champion_strategies.py` | Strategy registry (causal, exec-lag aware) |
| `common/ml/backtest_framework.py` | `resolve_cluster_params()` Phase 1/Phase 2 matcher |
| `scripts/tune_hyperparams.py` | Global per-algorithm tuning |
| `scripts/tune_cluster_hyperparams.py` | Per-cluster tuning |
| `scripts/ml/auto_tune.py` | Strategy-driven auto-tune (LGBM/CatBoost/XGBoost) |
| `scripts/run_champion_selection.py` | Champion selection (Stage 3) |
| `scripts/simulate_champion_strategies.py` | Strategy simulator (Stage 2) |
| `scripts/run_champion_experiment.py` | Champion-experiment runner (used by Champion Experiments tab) |
| `api/routers/platform/admin.py` | `POST /admin/tuning/invalidate-stale` |
| `api/routers/forecasting/backtest_management.py` | `POST /{model_id}/promote` (single + champion) |
| `api/routers/forecasting/tuning_chat.py` | Tuning chat backend |
| `config/forecast_pipeline_config.yaml` | `tuning:` + `champion:` sections (single source of truth) |
| `config/tune_strategies.yaml` | Per-model auto-tune strategy overrides |
| `config/cluster_tuning_profiles.yaml` | Per-cluster overrides (auto-generated) |
| `data/tuning/` | Trial JSON archive |
| `data/champion/dfu_assignments.csv` | Per-DFU champion assignments |
| `data/champion/champion_summary.json` | Strategy comparison summary |
