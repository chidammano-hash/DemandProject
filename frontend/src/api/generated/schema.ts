/**
 * Auto-generated OpenAPI types — placeholder.
 *
 * Gen-4 Roadmap (Frontend P0): this file is regenerated from the FastAPI
 * backend's live `/openapi.json` by running:
 *
 *   1. make api                 # start backend on :8000
 *   2. cd frontend
 *   3. npm run gen:types        # runs openapi-typescript -> schema.ts
 *
 * The script is defined in `frontend/package.json > scripts > gen:types`.
 * The tool `openapi-typescript` is a devDependency — if you see this comment
 * on a fresh checkout you still need to run `npm install` then `npm run gen:types`
 * before the real schema will be emitted into this file.
 *
 * Do NOT edit this file by hand. Do NOT import from it until types are generated.
 * Once generated, `components["schemas"]["<ModelName>"]` gives you typed
 * references to every Pydantic model exposed by the API.
 */

export type PlaceholderUntilGenerated = never;

export {};
